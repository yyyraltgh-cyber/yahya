/**
 * Icon size system. Three tiers, matching the convention documented at
 * the top of app/globals.css — import these instead of writing a raw
 * number, so a size choice is a documented decision, not a guess.
 */
export const ICON_INLINE = 14; // inline/badge/eyebrow icons (counts, meta rows)
export const ICON_CONTROL = 16; // standard control icons (buttons, list-item icons, nav icons)
export const ICON_FEATURED = 20; // empty states, quick-action tiles, card-header accents
